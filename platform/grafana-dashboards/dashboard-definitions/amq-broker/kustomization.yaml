apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

generatorOptions:
  disableNameSuffixHash: true
  labels:
    app: grafana
    dashboards: grafana
#  annotations:
#    note: generated

configMapGenerator:
- name: amq-broker-dashboards
  files:
    - amq-dashboard.json=./amq-dashboard.json
